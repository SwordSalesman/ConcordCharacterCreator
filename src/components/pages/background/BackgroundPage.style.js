import styled from "styled-components";
import { mediaSize } from "../../../styles/Global";

export const BackgroundPageWrapper = styled.div`
    @media (min-width: ${mediaSize.small}px) {
        width: 420px;
    }
`;
